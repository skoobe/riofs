before_install:
 - brew install glib libevent libxml2 openssl libmagic osxfuse
language: objective-c
compiler:
  - gcc
  - clang
script: 
  - ./autogen.sh
  - ./configure --enable-test-apps
  - make
notifications:
  irc:
    channels:
      - "irc.freenode.org#riofs"
    on_success: always
    on_failure: always
